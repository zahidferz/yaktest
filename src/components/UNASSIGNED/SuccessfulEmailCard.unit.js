import SuccessfulEmailCard from './SuccessfulEmailCard'

describe('@components/SuccessfulEmailCard', () => {
  it('exports a valid component', () => {
    expect(SuccessfulEmailCard).toBeAComponent()
  })
})
