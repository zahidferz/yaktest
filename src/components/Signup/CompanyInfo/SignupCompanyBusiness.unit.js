import SignupCompanyBusiness from './SignupCompanyBusiness'

describe('@components/SignupCompanyBusiness', () => {
  it('exports a valid component', () => {
    expect(SignupCompanyBusiness).toBeAComponent()
  })
})
