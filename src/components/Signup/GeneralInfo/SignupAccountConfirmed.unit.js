import SignupAccountConfirmed from './SignupAccountConfirmed'

describe('@components/Signup/GeneralInfo/SignupAccountConfirmed', () => {
  it('exports a valid component', () => {
    expect(SignupAccountConfirmed).toBeAComponent()
  })
})
