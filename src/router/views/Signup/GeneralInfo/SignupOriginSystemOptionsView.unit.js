import SignupOriginSystemOptionsView from './SignupOriginSystemOptionsView'

describe('@views/Signup/SignupOriginSystemOptionsView', () => {
  it('is a valid view', () => {
    expect(SignupOriginSystemOptionsView).toBeAViewComponent()
  })
})
