import SignupOriginSystemView from './SignupOriginSystemView'

describe('@views/Signup/SignupOriginSystemView', () => {
  it('is a valid view', () => {
    expect(SignupOriginSystemView).toBeAViewComponent()
  })
})
